{
  "name": "actions-register-source",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build:main": "ncc build src/index.ts -o dist/main",
    "build:cleanup": "ncc build src/cleanup.ts -o dist/cleanup",
    "build": "npm run build:main && npm run build:cleanup",
    "test": "node --test tests/**/*.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@types/node": "^22.8.7",
    "@vercel/ncc": "^0.34.0",
    "typescript": "^5.6.3"
  }
}

